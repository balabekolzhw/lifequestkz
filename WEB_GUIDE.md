# 🌐 Запуск LifeQuest в браузере (Chrome)

## ✅ Что было исправлено для веба:

### 1. Автоматическое определение платформы
```dart
static String get baseUrl {
  if (kIsWeb) {
    return 'http://localhost:3000';  // Для веб
  } else {
    return 'http://10.0.2.2:3000';   // Для Android
  }
}
```

### 2. Универсальная загрузка аватарки
- Теперь работает как на мобиле, так и в браузере
- Автоматически определяет формат файла (XFile)
- Правильный MIME type для всех платформ

## 🚀 Как запустить в Chrome:

### Шаг 1: Запустите backend
```bash
cd lifequest-backend
npm run dev
```

Должно появиться:
```
🎮 LifeQuest Server is running!
📡 Port: 3000
```

### Шаг 2: Запустите Flutter для веба
```bash
flutter run -d chrome
```

Или выберите Chrome в списке устройств:
```bash
flutter devices
flutter run -d <chrome-device-id>
```

### Шаг 3: Проверьте работу

1. **Регистрация/Вход** - должно работать ✅
2. **Создание задач** - новая красивая страница ✅
3. **Загрузка аватарки**:
   - Нажмите на иконку камеры 📷
   - Выберите фото
   - Должно загрузиться без ошибок ✅

## 🔧 Если возникают проблемы:

### Проблема: CORS ошибка
**Решение:** Backend уже настроен с `cors()`, но убедитесь что он запущен

### Проблема: "Connection refused"
**Решение:** Проверьте что backend работает на `localhost:3000`:
```bash
curl http://localhost:3000/api/health
```

Должен вернуть:
```json
{
  "success": true,
  "message": "LifeQuest API is running! 🚀"
}
```

### Проблема: Аватарка не загружается
**Решение 1:** Проверьте папку uploads создана:
```bash
ls -la lifequest-backend/uploads/avatars
```

**Решение 2:** Проверьте консоль браузера (F12) на ошибки

### Проблема: "Mixed Content" в Chrome
Если backend на HTTP, а фронт на HTTPS:
```bash
# Запускайте с флагом:
flutter run -d chrome --web-browser-flag "--disable-web-security"
```

## 📝 Особенности веб-версии:

✅ **Работает:**
- Регистрация и вход
- Создание задач
- Выполнение задач
- Поиск друзей
- Добавление друзей
- Рейтинг
- Загрузка аватарки (через галерею браузера)

⚠️ **Отличия от мобильной версии:**
- Вместо галереи телефона - выбор файла в браузере
- URL автоматически `localhost:3000` вместо `10.0.2.2:3000`

## 🎯 Быстрый старт:

```bash
# Терминал 1: Backend
cd lifequest-backend
npm run dev

# Терминал 2: Frontend
flutter run -d chrome
```

Готово! Откроется в Chrome автоматически 🎉

## 🛠️ Для production:

### Build веб-версии:
```bash
flutter build web
```

Файлы появятся в `build/web/`

### Запуск production:
Можете разместить на любом хостинге:
- Firebase Hosting
- Netlify
- Vercel
- GitHub Pages

**Важно:** Не забудьте изменить `baseUrl` на URL вашего backend!

